#Installation proccess

1 . clone the project
2. Run npm install on terminal for packages installation
3. update the VITE_APP_API_URL with api url on .env file
4. Run npm run dev to test the project. you should get localhost url like http://localhost:5173
5. Go to this url and you can login and register than can CRUD Tasks.

Note : The api url should be running
